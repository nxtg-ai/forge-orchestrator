# Synthetic regression fixture. Names and paths are intentionally non-production.
name: sample-memory-workspace
root: "~/projects/sample-memory"
template: claude-team-pane

before_start: []
after_start: []
on_pane_dead:
  - "echo pane-recovered >> /tmp/forge-fixture.log"

windows:
  - name: agent
    layout: tiled
    panes:
      - cwd: "~/projects/sample-memory"
