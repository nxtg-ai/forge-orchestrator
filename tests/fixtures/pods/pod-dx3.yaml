# Synthetic regression fixture. Names and paths are intentionally non-production.
name: pod-sample-service
root: "~/projects/sample-service"

before_start:
  - "echo preparing-sample"
after_start: []
on_pane_dead:
  - "echo pane-recovered >> /tmp/forge-fixture.log"

windows:
  - name: code
    layout: tiled
    panes:
      - cwd: "~/projects/sample-service"
        command: ""
  - name: api
    layout: tiled
    panes:
      - cwd: "~/projects/sample-service"
        command: ""
