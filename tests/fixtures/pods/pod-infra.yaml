# Synthetic two-project infrastructure regression fixture.
name: pod-sample-infra
root: "~/projects"

before_start: []
after_start: []
on_pane_dead:
  - "echo pane-recovered >> /tmp/forge-fixture.log"

windows:
  - name: infra
    layout: tiled
    panes:
      - cwd: "~/projects/sample-workflow"
      - cwd: "~/projects/sample-visualizer"
